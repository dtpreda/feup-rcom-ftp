#include <stdlib.h>
#include <stdio.h>
#include <regex.h>

#include "url/url.h"

int main(int argc, char* argv[]) {
    if (argc < 2) {
        printf("Wrong number of arguments\n");
        return -1;
    }

    return 0;
}